{
  "name": "viberadar-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf lib",
    "lint": "tsc --noEmit",
    "test": "ts-node --project tsconfig.json test/proxy.test.ts && ts-node --project tsconfig.json test/pairing.test.ts && ts-node --project tsconfig.json test/socialCounters.test.ts && ts-node --project tsconfig.json test/recency.test.ts && ts-node --project tsconfig.json test/usage.test.ts && ts-node --project tsconfig.json test/entitlements.test.ts && ts-node --project tsconfig.json test/soundcharts.test.ts",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^22.19.20",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
